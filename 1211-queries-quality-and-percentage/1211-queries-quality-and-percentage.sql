/* Write your PL/SQL query statement below */
select query_name, round(avg(rating/position),2) quality,ROUND(AVG(CASE WHEN rating < 3 THEN 1 ELSE 0 END) * 100, 2) poor_query_percentage from queries  group by query_name having query_name != 'null';